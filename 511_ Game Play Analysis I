"Table: Activity

+--------------+---------+
| Column Name  | Type    |
+--------------+---------+
| player_id    | int     |
------------------------
Solution:
# Write your MySQL query statement below
select distinct(player_id), min(event_date) as first_login
from Activity
group by player_id;
